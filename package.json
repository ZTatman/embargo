{
  "name": "myst",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "check": "tsc -b tsconfig.json",
    "clean": "pnpm -r clean"
  }
}
